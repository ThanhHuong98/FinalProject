import React from 'react';
import { View } from 'react-native';

const Separator = () => {
  return (
    <View
      style={{
        height: 0.5,
        width: '100%',
        backgroundColor: '#CED0CE',
      }}
    />
  );
};

export default Separator;
